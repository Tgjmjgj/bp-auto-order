
import { RandomOrderConfig, OrderItem } from '../../types/autoOrderConfigs';
import { KumirMenu } from '../../types/autoOrderMenus';


export const randomizeOrder = (menu: KumirMenu, conf: RandomOrderConfig): OrderItem[] | null => {
    

    return null;
};
